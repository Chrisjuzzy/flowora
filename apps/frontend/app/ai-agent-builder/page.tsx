"use client";

import SeoLandingPage from "@/components/SeoLandingPage";

export default function AIAgentBuilderPage() {
  return (
    <SeoLandingPage
      title="AI Agent Builder"
      headline="AI Agent Builder for Modern Teams"
      benefits={[
        "Design AI agents with prompts, tools, and memory in minutes.",
        "Launch agents into workflows, schedules, and the marketplace.",
        "Track execution quality, latency, and cost with built-in analytics.",
      ]}
    />
  );
}
